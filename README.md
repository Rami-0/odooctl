# odooctl

`odooctl` is a CLI-first, Odoo-aware deployment platform for self-hosted Odoo projects using Docker Compose.

It focuses on the operational workflow that generic deploy tools miss: PostgreSQL backups, filestore consistency, module updates, staging clones, sanitization, rollback, metadata, and health checks.

## Commands

```bash
odooctl init
odooctl init --dry-run
odooctl validate
odooctl doctor
odooctl project add acme --path /srv/odoo/acme
odooctl project list
odooctl env list
odooctl env create staging --clone-from production --sanitize
odooctl deploy staging --branch staging
odooctl deploy production --branch main
odooctl backup production
odooctl restore staging --backup latest
odooctl clone production staging --sanitize
odooctl update-modules staging --modules sale,stock,custom_module
odooctl rollback production --mode code
odooctl rollback production --mode full --backup production_2026-05-24_1600
odooctl logs staging --service odoo
odooctl logs staging --service odoo --no-follow --tail 100
odooctl status
odooctl status --environment production --json
odooctl schedule backup --env production --cron "0 2 * * *"
odooctl schedule doctor --env production --format cron --interval hourly
```

See `examples/odooctl.yml` for the starter config generated by `odooctl init`, `docs/examples.md` for command workflows, `examples/github-actions.yml` for a push-based deployment example, and `.github/workflows/odooctl-deploy.yml` for a checked-in manual deploy workflow.

## Install

For an operator install, use an isolated Python tool environment:

```bash
pipx install odooctl
# or
uv tool install odooctl
```

Then register a project and run preflight checks:

```bash
odooctl project add acme --path /srv/odoo/acme
odooctl -p acme doctor
```

See `docs/installation.md` for runtime prerequisites, Docker vs host PostgreSQL execution modes, optional extras, and development setup.

## Install for development

```bash
uv venv
uv pip install -e '.[dev]'
pytest -q
```

## Safety defaults

- Production deploys create database and filestore backups before deployment.
- Module update failures return non-zero and stop the deployment.
- Health check failures fail the deployment.
- Staging clone sanitization disables mail, fetchmail, crons, payment providers, and rewrites `web.base.url` by default.
- Secrets are referenced through environment variables such as `password_env`; do not commit secret values.

## Status

Control-plane milestones M6–M15 are complete: service layer, operation engine, import/takeover, promote, catalog, RBAC/secrets, local API + runner, web UI, domains/SSL, DR drills, and the migration assistant, on top of the M5 MVP foundation (Docker-native PostgreSQL/filestore operations, project/env management, secret redaction, optional S3 uploads, docs, examples, and tests). A security-hardening pass (2026-07) is in progress.
