# Configuration

Run `odooctl init` to create `odooctl.yml`. Keep secrets out of YAML. Use `password_env` and provider-specific environment variable references.

Key sections: `project`, `runtime`, `environments`, `postgres`, `odoo`, `backups`, `sanitization`, and `healthcheck`.
